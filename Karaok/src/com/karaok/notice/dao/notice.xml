<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://www.ibatis.com/dtd/sql-map-2.dtd" >

<sqlMap namespace="notice">
<typeAlias alias="Notice" type="com.karaok.notice.dto.NoticeDTO"/>

<insert id="insert" parameterClass="Notice">
insert into tb_notice
	values(seq_notice_num.nextval, #nickname#, #subject#, #contents#, sysdate, #hits#)

</insert>

  <delete id="delete" parameterClass="int">
	delete from tb_notice
	where num=#num#
  </delete>
  
  <update id="update" parameterClass="Notice">
	update tb_notice
	set subject=#subject#,contents=#contents#
	where num=#num#
  </update>
  
  <select id="select" parameterClass="int" resultClass="Notice">
	select  num, nickname, subject, contents, ndate, hits
	from tb_notice
	where num=#num#
  </select>
  
  <select id="selectAll" resultClass="Notice">
	select num, nickname, subject, contents, ndate, hits
	from tb_notice
	order by num desc
  </select>
  
   <select id="selectMAll" resultClass="Notice">
	select * from (select * from tb_notice order by num desc) where rownum between 1 and 5
  </select>
  
  

  <select id="selectPage" parameterClass="java.util.Map" resultClass="Notice">  
 select num,nickname,subject,contents,ndate,hits   
 	from  (select num,nickname,subject,contents,ndate,hits, rownum rn   
      from ( select num,nickname,subject,contents,ndate,hits    
           from tb_notice order by num desc))  
              where rn between #start# and #end#  
 </select>  
   
 <select id="selectCount" resultClass="int">  
 select count(*) from tb_notice  
 </select>  
  
 <update id="hitsUpdate" parameterClass="Notice">
 	update TB_notice
		set hits=hits+1
		where num=#num#
 </update>

</sqlMap>
